import LoginForm from "../components/forms/LoginForm.tsx";
export default function LoginPage() {
    return (
        <>
            <main className="flex h-full items-center justify-center overflow-hidden h-screen">
                <LoginForm/>
            </main>
        </>
    );
}