import LoginForm from "../components/forms/LoginForm.tsx";

export default function LoginPage() {
    return (
        <>
            <main className="flex items-center justify-center overflow-hidden h-screen">
                <LoginForm/>
            </main>
        </>
    );
}